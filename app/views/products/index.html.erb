<div>
    <h1>Search result for: "<%= params[:search] %>" <%= "in #{@category.name}" if @category&.name.present? %></h1>
    <div>
        <%= paginate @products %>
    </div>

    <%= render "home/products" %>
</div>