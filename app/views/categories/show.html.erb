<%= render "categories/categories_nav" %>

<div>
    <h2>Category: <%= @category.name %></h2>
    <p>Displaying <%= pluralize(@products.size, 'product') %> found</p>
</div>

<hr>

<div>
    <%= paginate @products %>
</div>

<%= render "home/products" %>